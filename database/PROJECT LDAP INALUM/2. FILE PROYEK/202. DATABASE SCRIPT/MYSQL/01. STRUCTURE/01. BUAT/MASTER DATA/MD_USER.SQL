CREATE TABLE MD_USER (
    ID_USER	    INT AUTO_INCREMENT,
    EMAIL	    VARCHAR	(100),
    NO_HP	    VARCHAR	(50),
    PASSWORD	VARCHAR	(100),
    NAMA	    VARCHAR	(100),
    KD_AKSES	INT,
    STATUS	    ENUM('A', 'N')    DEFAULT 'A',

    CONSTRAINT PK_ID_USER PRIMARY KEY (ID_USER)
);

ALTER TABLE MD_USER 
    ADD CONSTRAINT FK_KD_AKSES FOREIGN KEY (KD_AKSES) REFERENCES MD_USER(KD_AKSES);