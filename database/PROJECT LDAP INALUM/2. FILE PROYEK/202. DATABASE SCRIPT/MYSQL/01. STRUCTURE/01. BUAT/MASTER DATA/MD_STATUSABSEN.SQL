CREATE TABLE MD_STATUSABSEN(
	KD_STATUSABSEN	INT AUTO_INCREMENT,	
	NM_STATUSABSEN	VARCHAR	(100),
    STATUS	    	ENUM('A', 'N') DEFAULT 'A',
	KETERANGAN		VARCHAR	(1000),

	CONSTRAINT PK_KD_STATUSABSEN PRIMARY KEY (KD_STATUSABSEN)
);