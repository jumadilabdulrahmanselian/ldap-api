CREATE TABLE MD_PROVINSI (
    KD_PROVINSI	VARCHAR	(50),
    NM_PROVINSI	VARCHAR	(100),
    KETERANGAN	VARCHAR	(250),
    STATUS	    ENUM('A', 'N') DEFAULT 'A',

    CONSTRAINT PK_KD_PROVINSI PRIMARY KEY (KD_PROVINSI)
);