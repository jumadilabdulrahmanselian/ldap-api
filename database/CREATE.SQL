CREATE TABLE Xaaaa (
    ID_ADA INT PRIMARY KEY,
    NM_ADA VARCHAR(255) NOT NULL
);

CREATE TABLE MASALAH_BUAT_LO (
    ID_MANGGAMA INT PRIMARY KEY,
    NAMA VARCHAR(255) NOT NULL,
    TANGGAL DATE
);